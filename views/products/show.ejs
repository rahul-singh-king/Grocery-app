<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
</head>
<body>
    <h1><%= product.name %></h1>
    <ul>
        <li>Price: <%= product.price %></li>
        <li>cateogary: <%= product.cateogary %> </li>
    </ul>
    <div><a href='/product'>BACK</a></div>
    <a href="/product/<%= product._id %>/update">Update</a>
    <form action="/product/<%= product._id%>?_method=DELETE" method="POST">

        <button>Delete</button>
    </form>
   
</form>
</body>
</html>